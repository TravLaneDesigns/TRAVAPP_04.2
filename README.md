# Fun? Always! Road Trip!

React + Vite version 1 skeleton.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## GitHub Pages

Use GitHub Actions to run `npm install` and `npm run build`, then publish the `dist` folder.
